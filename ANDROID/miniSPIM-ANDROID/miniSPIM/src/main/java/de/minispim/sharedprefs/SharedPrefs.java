package de.minispim.sharedprefs;

import android.app.Activity;

/**
 * Created by Bene on 20.12.16.
 */

public class SharedPrefs extends Activity{
/*
    // Initialize shared preferences to save values permanently
    SharedPreferences prefs = this.getSharedPreferences("de.beamerscope", Context.MODE_PRIVATE);
    editor = prefs.edit();


    private Context context;

    public SavePref(Context context){
        this.context = context;
    }

    public void saveInt(String key, int value) {

        SharedPreferences sharedPref = getActivity().getPreferences(Context.MODE_PRIVATE);
        SharedPreferences.Editor editor = sharedPref.edit();
        editor.putInt(key, value);
        editor.commit();

    }
    */

}
